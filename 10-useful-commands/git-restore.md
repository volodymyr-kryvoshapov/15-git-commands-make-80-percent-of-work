# git restore

This git command will remove all files from staging area:

```
git restore --staged .
```

Or simply you can

```
git restore -S .
```

NOTE: Run these commands from your project root directory and don't forgot the . (dot) at the end!